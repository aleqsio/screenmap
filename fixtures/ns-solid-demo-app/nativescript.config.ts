import { NativeScriptConfig } from '@nativescript/core';

export default {
  id: 'org.screenmap.nssolid',
  appPath: 'src',
  appResourcesPath: 'App_Resources',
  bundler: 'vite',
  bundlerConfigPath: 'vite.config.mts'
} as NativeScriptConfig;
